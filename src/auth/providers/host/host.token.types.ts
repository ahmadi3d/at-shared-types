import type { PartoAccessTokenPayloadDto } from "../parto/parto.token.types";

/**
 * The current host bridge uses Parto-compatible access tokens.
 */
export type HostAccessTokenPayloadDto = PartoAccessTokenPayloadDto;
